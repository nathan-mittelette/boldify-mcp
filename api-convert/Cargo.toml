[package]
name    = "api-convert"
version = "0.1.0"
edition = "2021"

[dependencies]
parser       = { path = "../parser" }
service      = { path = "../service" }
lambda_http  = "1.1"
serde        = { version = "1", features = ["derive"] }
serde_json   = "1"
tokio        = { version = "1", features = ["full"] }
